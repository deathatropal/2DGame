package basics;

import java.awt.Graphics;
import java.awt.Graphics2D;
import java.awt.Image;

public class Block extends Coordinates
{
	public Image image;
	
	public Block(int x, int y)
	{
		this.x = x;
		this.y = y;
	}
	
	public boolean canMoveY(int playerX, int playerY, int imageHeight, int imageWidth, double futureMoveY)
	{
		if(playerX > x && playerX <= x + imageWidth)
		{
			System.out.println("True");
			if(playerY > y && futureMoveY <= y)
			{
				return false;
			}
			if(playerY < y && futureMoveY >= y)
			{
				return false;
			}
		}
		return true;
	}
	
	public boolean canMoveX(int playerX, int playerY, int imageHeight, int imageWidth, double futureMoveX)
	{
		if(playerY > y && playerY <= y + imageHeight)
		{
			System.out.println("True");
			if(playerX > x && futureMoveX <= x)
			{
				return false;
			}
			if(playerX < x && futureMoveX >= x)
			{
				return false;
			}
		}
		return true;
	}
	
	public void draw(Graphics graphics, Screen screen)
	{
		Graphics2D graphics2 = (Graphics2D) graphics;
		graphics2.drawImage(image, x, y, screen);
	}
}
